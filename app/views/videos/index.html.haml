%article.video_category
  %header
    %h3= link_to #{@videos.first.category}
  .videos.row
    - @videos.each do |video|
      .video.col-sm-2
        = link_to video do
          %img(src="/tmp/#{video.small_cover_url}")
